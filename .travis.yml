language:
  - php
php:
  - 7.0
  - 7.1
  - 7.2
  - 7.3
  - 7.4

os: linux

install:
  - composer install

script:
  - php vendor/bin/phpunit

after_success:
  - bash <(curl -s https://codecov.io/bash)